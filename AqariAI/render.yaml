services:
  - type: web
    name: aqariai-backend
    runtime: python
    buildCommand: pip install -r requirements.txt && python ingest.py
    startCommand: uvicorn api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GROQ_API_KEY
        sync: false
